{
  "name": "hnseasonsgarden",
  "private": true,
  "description": "PKD Hanoi Seasons Garden — static marketing site. Build only emits /css/tw.min.css from Tailwind sources.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/tailwind.css -o ./css/tw.min.css --minify",
    "watch:css": "tailwindcss -i ./src/tailwind.css -o ./css/tw.min.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "tailwindcss": "^3.4.17"
  }
}
